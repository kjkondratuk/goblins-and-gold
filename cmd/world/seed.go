package main

import (
	"os"
)

func main() {
	//game := voxel.NewVoxel(voxel.R_ROOT, "game", voxel.T_GAME)
	//game.SetChild(voxel.R_IS, voxel.NewVoxel(voxel.R_IS, "room", voxel.T_ROOM))

	os.Exit(0)
}
